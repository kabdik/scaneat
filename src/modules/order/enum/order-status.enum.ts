export enum OrderStatus {
  IDLE = 'idle',
  CANCELED = 'canceled',
  PENDING = 'pending',
  PROCESSING = 'processing',
  READY = 'ready',
  ON_DELIVERY = 'on_delivery',
  COMPLETED = 'completed',
}

export enum ChefOrderStatus {
  PENDING = 'pending',
  PROCESSING = 'processing',
  READY = 'ready',
}
